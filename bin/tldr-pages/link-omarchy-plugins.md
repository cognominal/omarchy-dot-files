# link-omarchy-plugins

> Symlink every third-party Omarchy plugin into `~/git/<user>---<repo>`.
> Personal script; see `link-omarchy-plugins --help`.

- Link (or re-check) all installed plugins:

`link-omarchy-plugins`
