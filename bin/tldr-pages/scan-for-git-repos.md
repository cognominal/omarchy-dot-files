# scan-for-git-repos

> Scan `$HOME` for git repos with a GitHub origin and symlink each into `~/git/<user>---<repo>`.
> Personal script; see `scan-for-git-repos --help`.

- Scan and link:

`scan-for-git-repos`
