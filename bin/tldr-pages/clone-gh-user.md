# clone-gh-user

> Interactively clone every public repo owned by a GitHub user into `~/git/<user>/<repo>`.
> Personal script; see `clone-gh-user --help`.

- Run interactively (prompts for a username via gum):

`clone-gh-user`
