# charm-git-repos

> Interactive TUI to browse a GitHub user's repos and clone/fetch/update them into `~/git`.
> Personal script; see `charm-git-repos --help`.

- Run interactively:

`charm-git-repos`
